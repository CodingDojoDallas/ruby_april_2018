# .include?(value) -- looks for input value and returns Boolean
names = ["bobby", "cindy", "billy"]
p names.include?('berta')
p names.include?('bobby')

# .last -- last value in array
p names.last 

# .max -- returns max value in range
p (1..10).max

# .min -- returns min value in range
p (2..20).min